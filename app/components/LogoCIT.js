export default function LogoCIT({ height = 48 }) {
  return (
    <img
      src="/cit-logo.png"
      alt="CI&T"
      style={{ height, width: "auto", display: "block", userSelect: "none" }}
    />
  );
}
